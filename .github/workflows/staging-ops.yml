name: Staging ops (manual)

# Manually-dispatched runner for STAGING-ONLY operations used by the master-upgrade
# QA gates. Two jobs, deliberately separated:
#   tests — checks out the ref under test and runs npm install + the Deno edge suite.
#           It has NO secrets, so untrusted branch code can never see staging keys.
#   ops   — fresh runner. Checks out the ref but executes NO repository scripts
#           (no npm install, no package.json scripts): only the Supabase CLI (deploy),
#           psql (one migration file) and the orchestrator-authored check script.
# Never gates anything and never touches production: the project ref is hard-coded
# to staging and every input and the migration file are refused if they contain the
# production ref. Lovable's flow is untouched.
#
# Required repository secrets (staging values only):
#   SUPABASE_ACCESS_TOKEN, STAGING_DB_POOLER_URL, STAGING_SERVICE_ROLE_KEY,
#   STAGING_ANON_KEY, STAGING_INTERNAL_API_SECRET (optional)

on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Branch / SHA under test to check out (blank = the ref this workflow was dispatched on)"
        required: false
        default: ""
      functions:
        description: "Edge functions to deploy to STAGING (comma-separated, blank = none)"
        required: false
        default: ""
      migration_file:
        description: "One migration file under supabase/migrations/ to apply to STAGING with psql (blank = none)"
        required: false
        default: ""
      apply_twice:
        description: "Apply the migration a second time to prove it is idempotent"
        type: boolean
        default: false
      edge_tests:
        description: "Run npm run test:edge (Deno) in the secretless tests job"
        type: boolean
        default: true
      check_script:
        description: "Bash to run after deploy (env: SUPABASE_URL, SERVICE_ROLE_KEY, ANON_KEY, DB_URL, INTERNAL_API_SECRET; psql + curl + jq available)"
        required: false
        default: ""
      report_pr:
        description: "PR number to post the ops report to as a comment (blank = none)"
        required: false
        default: ""

concurrency:
  group: staging-ops
  cancel-in-progress: false

permissions: {}

env:
  STAGING_REF: jhipmqdpjenojfhfjgzq
  SUPABASE_URL: https://jhipmqdpjenojfhfjgzq.supabase.co
  # The production ref is deliberately never written out in this file (the repo's
  # noProdRefInStagingSql guard forbids it in any staging-named file); it is assembled
  # from two halves at runtime for the refusal checks only.
  PROD_A: ntzeiflqq
  PROD_B: luwgdfmatjh

jobs:
  guard:
    name: Refuse anything that names production
    runs-on: ubuntu-latest
    permissions: {}
    steps:
      - env:
          INPUTS_JSON: ${{ toJSON(github.event.inputs) }}
        run: |
          if printf '%s' "$INPUTS_JSON" | grep -q "${PROD_A}${PROD_B}"; then
            echo "::error::Inputs reference the PRODUCTION project ref. Refusing."; exit 1; fi
          echo "inputs OK (staging only)"

  tests:
    name: Deno edge suite (no secrets)
    needs: guard
    if: ${{ inputs.edge_tests }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref != '' && inputs.ref || github.ref }}
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: npm install --no-audit --no-fund
      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x
      - name: Deno edge suite
        id: edge
        run: |
          set +e
          npm run test:edge 2>&1 | tee /tmp/edge.txt
          echo "${PIPESTATUS[0]}" > /tmp/edge.status
          exit 0
      # The Deno suite is the ONE suite the build sandbox cannot run (deno.land is
      # network-blocked there), so its output is the only place some failures ever
      # appear. Actions logs are not readable by the orchestrator, so post it to the
      # PR whether it passed or failed, then fail the job on the captured status.
      - name: Post Deno suite output to PR
        if: ${{ always() && inputs.report_pr != '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ inputs.report_pr }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          python3 - "$PR" "$RUN_URL" <<'PY'
          import json,os,subprocess,sys
          pr,run_url=sys.argv[1],sys.argv[2]
          try: out=open('/tmp/edge.txt',errors='replace').read()
          except FileNotFoundError: out='(no output captured)'
          try: status=open('/tmp/edge.status').read().strip()
          except FileNotFoundError: status='?'
          head='passed' if status=='0' else f'FAILED (exit {status})'
          body=f"### Deno edge suite \u2014 {head}\n{run_url}\n\n```\n{out[-55000:]}\n```"
          repo=os.environ['GITHUB_REPOSITORY']
          subprocess.run(['gh','api','repos/'+repo+'/issues/'+pr+'/comments',
                          '-f','body='+body],check=False)
          PY
      - name: Fail if the Deno suite failed
        if: ${{ always() }}
        run: test "$(cat /tmp/edge.status 2>/dev/null || echo 1)" = "0"

  ops:
    name: Staging-only deploy / migrate / checks
    needs: [guard, tests]
    # run even when tests were skipped (edge_tests=false), never when they failed
    if: ${{ always() && needs.guard.result == 'success' && (needs.tests.result == 'success' || needs.tests.result == 'skipped') }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout ref under test (no repository scripts are executed in this job)
        uses: actions/checkout@v4
        with:
          ref: ${{ inputs.ref != '' && inputs.ref || github.ref }}

      - name: Set up Supabase CLI
        uses: supabase/setup-cli@v1
        with:
          version: latest

      - name: Deploy edge functions to STAGING
        if: ${{ inputs.functions != '' }}
        env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          test -n "$SUPABASE_ACCESS_TOKEN" || { echo "::error::SUPABASE_ACCESS_TOKEN secret missing"; exit 1; }
          IFS=',' read -ra FNS <<< "${{ inputs.functions }}"
          for f in "${FNS[@]}"; do
            f="$(echo "$f" | xargs)"; [ -z "$f" ] && continue
            test -d "supabase/functions/$f" || { echo "::error::no such function: $f"; exit 1; }
            echo "== deploying $f to $STAGING_REF" | tee -a /tmp/report.txt
            supabase functions deploy "$f" --project-ref "$STAGING_REF" 2>&1 | tee -a /tmp/report.txt
            test "${PIPESTATUS[0]}" = "0"
          done

      - name: Apply migration to STAGING (psql)
        if: ${{ inputs.migration_file != '' }}
        env:
          DB_URL: ${{ secrets.STAGING_DB_POOLER_URL }}
          MIG: ${{ inputs.migration_file }}
        run: |
          test -n "$DB_URL" || { echo "::error::STAGING_DB_POOLER_URL secret missing"; exit 1; }
          case "$DB_URL" in *jhipmqdpjenojfhfjgzq*) ;; *) echo "::error::DB URL is not the staging pooler"; exit 1;; esac
          case "$MIG" in */*|*..*) echo "::error::migration_file must be a bare filename"; exit 1;; esac
          F="supabase/migrations/$MIG"
          test -f "$F" || { echo "::error::no such migration: $F"; exit 1; }
          grep -q "${PROD_A}${PROD_B}" "$F" && { echo "::error::migration contains the production ref"; exit 1; }
          echo "== applying $F (1st run)" | tee -a /tmp/report.txt
          psql "$DB_URL" -v ON_ERROR_STOP=1 -f "$F" 2>&1 | tee -a /tmp/report.txt; test "${PIPESTATUS[0]}" = "0"
          if [ "${{ inputs.apply_twice }}" = "true" ]; then
            echo "== applying $F (2nd run, must be a no-op)" | tee -a /tmp/report.txt
            psql "$DB_URL" -v ON_ERROR_STOP=1 -f "$F" 2>&1 | tee -a /tmp/report.txt; test "${PIPESTATUS[0]}" = "0"
          fi
          V="${MIG%%_*}"
          psql "$DB_URL" -v ON_ERROR_STOP=1 -c "INSERT INTO supabase_migrations.schema_migrations (version, name) SELECT '$V', '$MIG' WHERE NOT EXISTS (SELECT 1 FROM supabase_migrations.schema_migrations WHERE version = '$V');" || echo "::warning::could not record migration in schema_migrations"

      - name: QA check script (authored by the dispatcher, not the branch)
        if: ${{ inputs.check_script != '' }}
        env:
          SERVICE_ROLE_KEY: ${{ secrets.STAGING_SERVICE_ROLE_KEY }}
          ANON_KEY: ${{ secrets.STAGING_ANON_KEY }}
          DB_URL: ${{ secrets.STAGING_DB_POOLER_URL }}
          INTERNAL_API_SECRET: ${{ secrets.STAGING_INTERNAL_API_SECRET }}
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          CHECK_SCRIPT: ${{ inputs.check_script }}
        run: |
          printf '%s\n' "$CHECK_SCRIPT" > /tmp/check.sh
          grep -q "${PROD_A}${PROD_B}" /tmp/check.sh && { echo "::error::check script references production"; exit 1; }
          echo "== check script" | tee -a /tmp/report.txt
          bash -euo pipefail /tmp/check.sh 2>&1 | tee -a /tmp/report.txt; test "${PIPESTATUS[0]}" = "0"

      - name: Post report to PR
        if: ${{ always() && inputs.report_pr != '' }}
        env:
          GH_TOKEN: ${{ github.token }}
          PR: ${{ inputs.report_pr }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          OUTCOME: ${{ job.status }}
          S1: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
          S2: ${{ secrets.STAGING_DB_POOLER_URL }}
          S3: ${{ secrets.STAGING_SERVICE_ROLE_KEY }}
          S4: ${{ secrets.STAGING_ANON_KEY }}
          S5: ${{ secrets.STAGING_INTERNAL_API_SECRET }}
        run: |
          # Redact every secret value explicitly (exact-match replacement, plus the DB password
          # on its own), then generic JWT / postgres-URL / sbp_ token patterns.
          python3 - <<'PY'
          import os,re
          try: body=open('/tmp/report.txt',errors='replace').read()
          except FileNotFoundError: body='(no report captured)'
          vals=[os.environ.get(k,'') for k in ('S1','S2','S3','S4','S5')]
          m=re.match(r'postgresql://[^:]+:([^@]+)@',os.environ.get('S2',''))
          if m: vals.append(m.group(1))
          for v in sorted({v for v in vals if len(v)>=6},key=len,reverse=True): body=body.replace(v,'<redacted>')
          body=re.sub(r'eyJ[A-Za-z0-9_.-]{20,}','<jwt>',body)
          body=re.sub(r'postgres(ql)?://\S+','<db-url>',body)
          body=re.sub(r'sbp_[A-Za-z0-9_]+','<sbp>',body)
          open('/tmp/report.redacted.txt','w').write(body[-60000:])
          PY
          BODY="$(cat /tmp/report.redacted.txt)"
          python3 - "$PR" "$RUN_URL" "$OUTCOME" "$BODY" <<'PY'
          import json,os,sys,urllib.request
          pr,url,outcome,body=sys.argv[1:5]
          text=f"### Staging ops run — {outcome}\n{url}\n\n```\n{body}\n```"
          req=urllib.request.Request(f"https://api.github.com/repos/{os.environ['GITHUB_REPOSITORY']}/issues/{pr}/comments",data=json.dumps({"body":text}).encode(),headers={"Authorization":"Bearer "+os.environ["GH_TOKEN"],"Accept":"application/vnd.github+json","Content-Type":"application/json"},method="POST")
          urllib.request.urlopen(req)
          PY
